import React from 'react'
import Modal from './Modal'

export default function ViewResultsModal({isOpen,close}) {
  return (
    <Modal isOpen={isOpen} close={close} heading={"View Results"} >
        
    </Modal>
  )
}
